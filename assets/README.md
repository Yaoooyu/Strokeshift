# Assets

The current PNG files are synthetic, publication-style placeholders generated by `scripts/generate_assets.py`. They contain no patient data, clinical images, report text, or patient-level embeddings.

Before release, replace them with the final manuscript figures only after privacy review. A domain-shift embedding figure can remain sensitive even after removing names because individual points may encode patient-level information.
